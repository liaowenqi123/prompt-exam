{
  "name": "prompt-exam",
  "private": true,
  "version": "0.1.0",
  "description": "Prompt Exam - 提示词考试：用真实场景 + 大模型评分来检验提示词质量",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run start -w server",
    "setup": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
